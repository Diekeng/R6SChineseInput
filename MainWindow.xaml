<Window x:Class="MouseHookApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MouseHookApp" Height="500" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="True">

    <Border CornerRadius="18" Margin="8" Padding="0">
        <Border.Background>
            <ImageBrush ImageSource="bg.png" Stretch="UniformToFill"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Color="#FF80405A" BlurRadius="16" Opacity="0.35" ShadowDepth="4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏（可拖动） -->
            <Grid Grid.Row="0" Background="#CC884466" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.ColumnSpan="2"
                        Background="#CC884466"
                        CornerRadius="18,18,0,0"/>

                <TextBlock Grid.Column="0" Text="🌈 R6SChineseInput"
                           FontSize="14" FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="14,0,0,0">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                    </TextBlock.Effect>
                </TextBlock>

                <Button Grid.Column="1" Content="━" 
                        FontSize="12" FontWeight="Bold"
                        Width="32" Height="28"
                        Margin="0,0,6,0"
                        Background="Transparent" Foreground="White"
                        BorderThickness="0" Cursor="Hand"
                        Click="MinimizeButton_Click"
                        ToolTip="隐藏到托盘">
                    <Button.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                    </Button.Effect>
                </Button>
            </Grid>

            <!-- 状态 + 热键设置 -->
            <StackPanel Grid.Row="1" Margin="14,14,14,8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                    <TextBlock Text="🌈" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusText" Text="运行中" FontSize="14" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"
                               Margin="8,0,0,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Opacity="0.9"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <!-- 热键选择 -->
                <Grid HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="热键:" FontSize="12" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"
                               Margin="0,0,8,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <ComboBox x:Name="ModifierCombo" Grid.Column="1"
                              FontSize="12" Width="70" Height="26"
                              Margin="0,0,6,0"
                              SelectionChanged="HotkeyCombo_SelectionChanged"/>

                    <TextBlock Grid.Column="2" Text="+" FontSize="14" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"
                               Margin="2,0,8,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <ComboBox x:Name="KeyCombo" Grid.Column="3"
                              FontSize="12" Width="100" Height="26"
                              SelectionChanged="HotkeyCombo_SelectionChanged"/>
                </Grid>
            </StackPanel>

            <!-- 日志区域 -->
            <Border Grid.Row="3" Background="#40000000" CornerRadius="10"
                    Margin="12,6,12,12" Padding="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="8,4,4,4">
                        <TextBlock Text="📋 日志" FontSize="12" FontWeight="Bold"
                                   Foreground="White">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <Button x:Name="ClearButton" Content="✕"
                                HorizontalAlignment="Right"
                                FontSize="12" Padding="6,1"
                                Background="Transparent" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="ClearButton_Click"
                                ToolTip="清空日志">
                             <Button.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                            </Button.Effect>
                        </Button>
                    </Grid>

                    <ListBox x:Name="LogListBox" Grid.Row="1"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="White"
                             FontFamily="Consolas"
                             FontSize="12"
                             FontWeight="SemiBold"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="6,2"/>
                                <Setter Property="Margin" Value="0,1"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}"
                                                    CornerRadius="4">
                                                <ContentPresenter>
                                                     <ContentPresenter.Effect>
                                                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Opacity="1"/>
                                                    </ContentPresenter.Effect>
                                                </ContentPresenter>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#30FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Grid>
            </Border>

            <!-- 退出按钮 -->
            <Button Grid.Row="4" Content="退出程序"
                    FontSize="11" Padding="16,6"
                    Margin="0,0,0,12"
                    HorizontalAlignment="Center"
                    Background="#44FFFFFF" Foreground="#AA3E2723"
                    BorderThickness="0" Cursor="Hand"
                    Click="ExitButton_Click"/>
        </Grid>
    </Border>
</Window>
